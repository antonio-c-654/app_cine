<%- include('partials/header') %>
<%- include('partials/nav_bar_logout') %>

<div class="bg-green-200 w-screen h-[90vh] flex flex-col items-center justify-center gap-4">

    <h1 class="text-2xl">Lista de peliculas</h1>
    
    <div class="flex flex-row flex-wrap gap-4">

    <% for (let i = 0; i < objetos.length; i++) { %>
        <div class="border rounded-md flex flex-col items-center p-2 bg-white">
            <p><%= objetos[i].nombre %></p>
            <img src="<%= objetos[i].portada %>" class="w-[150px]">
        </div>
    <% } %>

    </div>

</div>

<%- include('partials/footer') %>